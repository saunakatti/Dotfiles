

local rosewater = "rgb(f5e0dc)"
local rosewaterAlpha = "rgba(f5e0dc, 0.8)"

local flamingo = "rgb(f2cdcd)"
local flamingoAlpha = "rgba(f2cdcd, 0.8)"

local pink = "rgb(f5c2e7)"
local pinkAlpha = "rgba(f5c2e7, 0.8)"

local mauve = "rgb(cba6f7)"
local mauveAlpha = "rgba(cba6f7, 0.8)"

local red = "rgb(f38ba8)"
local redAlpha = "rgba(f38ba8, 0.8)"

local maroon = "rgb(eba0ac)"
local maroonAlpha = "rgba(eba0ac, 0.8)"

local peach = "rgb(fab387)"
local peachAlpha = "rgba(fab387, 0.8)"

local yellow = "rgb(f9e2af)"
local yellowAlpha = "rgba(f9e2af, 0.8)"

local green = "rgb(a6e3a1)"
local greenAlpha = "rgba(a6e3a1, 0.8)"

local teal = "rgb(94e2d5)"
local tealAlpha = "rgba(94e2d5, 0.8)"

local sky = "rgb(89b4fa)"
local skyAlpha = "rgba(89b4fa, 0.8)"

local sapphire = "rgb(74c7ec)"
local sapphireAlpha = "rgba(74c7ec, 0.8)"

local blue = "rgb(89b4fa)"
local blueAlpha = "rgba(89b4fa, 0.8)"

local lavender = "rgb(b4befe)"
local lavenderAlpha = "rgba(b4befe, 0.8)"

local text = "rgb(cdd6f4)"
local textAlpha = "rgba(cdd6f4, 0.8)"

local subtext1 = "rgb(bac2de)"
local subtext1Alpha = "rgba(bac2de, 0.8)"

local subtext0 = "rgb(a6adc8)"
local subtext0Alpha = "rgba(a6adc8, 0.8)"

local subtext2 = "rgb(9399b2)"
local subtext2Alpha = "rgba(9399b2, 0.8)"

local overlay2 = "rgb(9399b2)"
local overlay2Alpha = "rgba(9399b2, 0.8)"

local overlay1 = "rgb(7f849c)"
local overlay1Alpha = "rgba(7f849c, 0.8)"

local overlay0 = "rgb(6c7086)"
local overlay0Alpha = "rgba(6c7086, 0.8)"

local surface2 = "rgb(585b70)"
local surface2Alpha = "rgba(585b70, 0.8)"

local surface1 = "rgb(45475a)"
local surface1Alpha = "rgba(45475a, 0.8)"

local surface0 = "rgb(313244)"
local surface0Alpha = "rgba(313244, 0.8)"

local base = "rgb(1e1e2e)"
local baseAlpha = "rgba(1e1e2e, 0.8)"

local mantle = "rgb(181825)"
local mantleAlpha = "rgba(181825, 0.8)"

local crust = "rgb(11111b)"
local crustAlpha = "rgba(11111b, 0.8)"

mocha = {
    rosewater = rosewater,
    flamingo = flamingo,
    pink = pink,
    mauve = mauve,
    red = red,
    maroon = maroon,
    peach = peach,
    yellow = yellow,
    green = green,
    teal = teal,
    sky = sky,
    sapphire = sapphire,
    blue = blue,
    lavender = lavender,
    text = text,
    subtext1 = subtext1,
    subtext0 = subtext0,
    subtext2 = subtext2,
    overlay2 = overlay2,
    overlay1 = overlay1,
    overlay0 = overlay0,
    surface2 = surface2,
    surface1 = surface1,
    surface0 = surface0,
    base = base,
    mantle = mantle,
    crust = crust
}